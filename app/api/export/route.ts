import {database,json,identity,failure,periodSchema,HttpError,premium} from '@/lib/server';
export async function GET(req:Request){try{const user=await identity();if(!(await premium(user.userId)).active)throw new HttpError(403,'Ekspor CSV tersedia di Premium.');const period=periodSchema.parse(new URL(req.url).searchParams.get('period'));const rows=await database().prepare('SELECT date,type,note,pocket,amount FROM transactions WHERE user=? AND period=? ORDER BY date').bind(user.userId,period).all<Record<string,string|number>>();const cell=(v:unknown)=>{let s=String(v??'');if(/^[=+@\-\t\r]/.test(s))s="'"+s;return '"'+s.replaceAll('"','""')+'"';};const csv='\uFEFF'+[['Tanggal','Jenis','Catatan','Kantong','Nominal'],...rows.results.map(r=>[r.date,r.type,r.note,r.pocket,r.amount])].map(r=>r.map(cell).join(',')).join('\r\n');return new Response(csv,{headers:{'Content-Type':'text/csv; charset=utf-8','Content-Disposition':`attachment; filename="kantong-rantau-${period}.csv"`,'Cache-Control':'no-store'}});}catch(e){return failure(e);}}
