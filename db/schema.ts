import {sqliteTable,text,integer,primaryKey,index} from 'drizzle-orm/sqlite-core';
export const budgets=sqliteTable('budgets',{user:text('user').notNull(),period:text('period').notNull(),data:text('data').notNull()},t=>[primaryKey({columns:[t.user,t.period]})]);
export const transactions=sqliteTable('transactions',{id:text('id').primaryKey(),user:text('user').notNull(),period:text('period').notNull(),date:text('date').notNull(),type:text('type').notNull(),amount:integer('amount').notNull(),pocket:text('pocket').notNull(),note:text('note').notNull(),debtId:text('debt_id')},t=>[index('idx_transactions_user_period').on(t.user,t.period),index('idx_transactions_user_debt').on(t.user,t.debtId)]);
export const debts=sqliteTable('debts',{id:text('id').primaryKey(),user:text('user').notNull(),data:text('data').notNull()},t=>[index('idx_debts_user').on(t.user)]);
export const orders=sqliteTable('orders',{id:text('id').primaryKey(),user:text('user').notNull(),amount:integer('amount').notNull(),status:text('status').notNull(),url:text('url'),created:integer('created').notNull(),expires:integer('expires'),mode:text('mode').notNull()},t=>[index('idx_orders_user_created').on(t.user,t.created)]);
